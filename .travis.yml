os: osx

osx_image: 
  - xcode11
  - xcode10.1
  - xcode9.2

addons:
  homebrew:
    update: true
    packages:
      - bash
      - coreutils
      - findutils
      - jq
      - hub

script: ./.travis-build.sh

